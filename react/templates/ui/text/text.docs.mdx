---
description: 'Renders text in a p tag'
labels: ['paragraph']
---

import { Text } from './text';

## Text Component

A basic `<p>` tag that renders some text

### Component usage

```js
<Text text="hello from Text" />
```

### Using props to customize the text

Modify the text to see it change live:

```js live
<Text text="hello from Text" />
```
